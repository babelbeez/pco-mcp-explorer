{
  "name": "pco-mcp-explorer-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CORS proxy worker for the PCO MCP Explorer (allowlisted pass-through to the Planning Center MCP server).",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250617.0",
    "typescript": "~5.6.3",
    "wrangler": "^4.118.0"
  }
}
